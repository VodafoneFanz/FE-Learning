    /*
        JS注释
        1.JS严格区分大小写；
        2.JS每条语句分号结尾；
        3.JS自动忽略多个空格和换行；
    */

    /*
        字面量和变量
        1.字面量，不可以改变的量，eg. 1 2 3 4
            字面两可以直接使用，但是不会直接使用
        变量 用来保存字面量 而且可以任意改变
            使用更加方便，开发过程中通过变量保存字面量
    */

    //声明变量 var
    var a;
    //为变量赋值
    a = 123;
    //声明和赋值同时
    b = 789;

    console.log(a);

    /*
        标识符 token
        所有可以由我们自主命名的都成为标识符
        eg 变量名 函数名 属性名
        标识符命名规则：
            1.可以含有字母 数字 _ $
            2.不能用数字作为开头；
            3.不能使用ES中的关键字或保留字
            4.标识符一般采用驼峰命名
                首字母小写 每个单词开头大写 其余小写
                eg xxxHolic helloWorld

        JS底层保存标识符采用Unicode编码，所以理论上所有utf-8内容都可以作为标识符
    */
